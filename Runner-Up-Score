if __name__ == '__main__':
    n = int(input())
    arr = list(map(int, input().split()))
    arr.sort(reverse=True)
    max=arr[0]
    Length=len(arr)-1
    #print(max)
    for i in arr:
        if i<max:
            print(i)
            break
        
        
